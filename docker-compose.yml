  version: '3.8'

  services: 
    forum: 
      build: .
      ports: 
      - "8080:8080"
      volumes: 
      - ./database:/app/database
      - ./web/static/uploads:/app/web/static/uploads
      restart: unless-stopped